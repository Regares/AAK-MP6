#include <stdio.h>
#include <math.h>

int main() 
{
    int A;
    float res;
    printf("Введите значение A: ");
    scanf("%d", &A);
    res = pow((A * A * A * A), 2);
    printf("Значение выражения равно %f", res);
    return 0;
}
