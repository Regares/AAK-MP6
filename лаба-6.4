#include <stdio.h>
#include <math.h>

int main() 
{
    int x;
    float y;
    printf("Введите значение X: ");
    scanf("%d", &x);
    y = (3 * pow(x, 6) - 6 * pow(x, 2) - 7);
    printf("Значение выражения равно %f", y);
    return 0;
}
