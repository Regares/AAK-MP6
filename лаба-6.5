#include <stdio.h>
#include <math.h>

int main() 
{
    int x;
    float y;
    printf("Введите значение X: ");
    scanf("%d", &x);
    y = (4 * pow((x - 3), 6) - 7 * pow((x - 3), 3) + 2);
    printf("Значение выражения равно %f", y);
    return 0;
}
